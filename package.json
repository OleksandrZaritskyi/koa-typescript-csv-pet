{
  "name": "koa-typescript-csv-pet",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up",
    "dev:build": "docker compose -f docker-compose.dev.yml up --build",
    "prod": "docker compose -f docker-compose.prod.yml up",
    "prod:build": "docker compose -f docker-compose.prod.yml up --build",
    "logs": "docker compose -f docker-compose.dev.yml logs -f",
    "logs:api": "docker compose -f docker-compose.dev.yml logs -f api",
    "logs:web": "docker compose -f docker-compose.dev.yml logs -f web",
    "db": "docker compose -f docker-compose.dev.yml exec postgres psql -U postgres -d csv_app",
    "migrate": "docker compose -f docker-compose.dev.yml exec api npm run migrate:up",
    "migrate:down": "docker compose -f docker-compose.dev.yml exec api npm run migrate:down",
    "migrate:create": "docker compose -f docker-compose.dev.yml exec api npm run migrate:create --"
  }
}
